<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>Status dos Correios</title>
  <style>
    body {
      background-color: <%= greve ? '#ffe6e6' : '#e6ffe6' %>;
      color: #333;
      font-family: Arial, sans-serif;
      padding: 40px;
      max-width: 700px;
      margin: 40px auto;
      box-sizing: border-box;
    }
    h1 {
      font-size: 2.8em;
      color: <%= greve ? '#c00' : '#070' %>;
      text-align: center;
      margin-bottom: 30px;
    }
    .noticia {
      background-color: #fff;
      border: 1px solid #ccc;
      border-left: 6px solid <%= greve ? '#c00' : '#070' %>;
      padding: 20px 25px;
      border-radius: 10px;
      box-shadow: 0 0 8px rgba(0,0,0,0.1);
    }
    .noticia h2 {
      margin: 0 0 12px;
      font-size: 1.5em;
    }
    .noticia h2 a {
      text-decoration: none;
      color: #0066cc;
      transition: color 0.3s ease;
    }
    .noticia h2 a:hover {
      color: #004999;
    }
    .noticia p {
      margin-top: 10px;
      color: #555;
      line-height: 1.4;
      font-size: 1.1em;
    }
    .sem-noticia {
      font-size: 1.2em;
      color: #666;
      text-align: center;
      margin-top: 30px;
    }
  </style>
</head>
<body>
  <h1>
    <% if (greve) { %>
      🚫 Os Correios estão em greve.
    <% } else { %>
      ✅ Os Correios estão funcionando normalmente.
    <% } %>
  </h1>

  <% if (noticia) { %>
    <div class="noticia">
      <h2><a href="<%= noticia.link %>" target="_blank" rel="noopener noreferrer"><%= noticia.titulo %></a></h2>
      <p><%= noticia.resumo %></p>
    </div>
  <% } else { %>
    <div class="sem-noticia">Nenhuma notícia recente encontrada.</div>
  <% } %>
</body>
</html>
